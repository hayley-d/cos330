<div class="min-h-screen flex">
  <div class="hidden md:flex w-1/2">
    <%= image_tag "login-bg.jpg", class: "object-cover w-full h-full" %>
  </div>

  <div class="flex w-full md:w-1/2 items-center justify-center bg-gray-100">
    <div class="bg-white p-8 rounded shadow-md w-96">
      <h1 class="text-2xl font-bold mb-6">Register</h1>

      <%= form_with url: register_path, method: :post, local: true do |f| %>
        <%= f.text_field :first_name, placeholder: "First Name", class: "w-full mb-4 border rounded p-2" %>
        <%= f.text_field :last_name, placeholder: "Last Name", class: "w-full mb-4 border rounded p-2" %>
        <%= f.email_field :email, placeholder: "Email", class: "w-full mb-4 border rounded p-2" %>
        <%= f.password_field :password, placeholder: "Password", class: "w-full mb-4 border rounded p-2" %>

        <%= f.submit "Sign Up", class: "w-full bg-green-600 text-white py-2 rounded hover:bg-green-700" %>
      <% end %>

      <p class="mt-4 text-sm text-center">
        Already have an account?
        <%= link_to "Login", login_path, class: "text-blue-600 hover:underline" %>
      </p>
    </div>
  </div>
</div>
